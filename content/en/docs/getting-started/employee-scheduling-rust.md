---
title: 'Employee Scheduling'
linkTitle: 'Employee Scheduling'
icon: fa-brands fa-rust
date: 2026-04-05
weight: 5
description:
  "Build an employee scheduler with the current SolverForge 0.8.5 runtime
  surface"
categories: [Quickstarts]
tags: [quickstart, rust]
---

{{% pageinfo color="primary" %}} This guide is written against the current
**SolverForge 0.8.5** runtime surface: generated constraint-stream accessors,
`solver.toml`, and `SolverEvent`-based solving through `SolverManager`.
{{% /pageinfo %}}

## Overview

This guide is the recommended first tutorial for a Rust employee-scheduling
application with SolverForge.

It walks through the current runtime surface:

1. Model employees as problem facts and shifts as planning entities
2. Write constraints with generated `ConstraintFactory` accessors
3. Run the solver and consume `SolverEvent` updates
4. Tune the search in `solver.toml`

If you want the fastest path to a runnable project instead of building along
with the docs, use the standalone `solverforge-cli` scaffold.

## Scaffolded starting point

The standalone CLI is now the default onboarding path:

```bash
cargo install solverforge-cli
solverforge new clinic-scheduler
cd clinic-scheduler
solverforge server
```

That scaffold gives you the current application shape:

- A project that depends on the `solverforge` facade crate
- A neutral shell you can extend with generator commands
- Domain modules for entities, facts, and DTOs
- A `solver.toml` file loaded automatically by the stock runtime
- A service layer built around `SolverManager`

The rest of this guide explains the runtime concepts behind that scaffold.

## The Domain Model

For employee scheduling, the immutable inputs are employees. The things the
solver changes are shifts.

```rust
use solverforge::prelude::*;

#[problem_fact]
pub struct Employee {
    #[planning_id]
    pub id: usize,
    pub name: String,
    pub skills: Vec<String>,
}

#[planning_entity]
pub struct Shift {
    #[planning_id]
    pub id: usize,
    pub required_skill: String,
    pub start: i64,
    pub end: i64,

    #[planning_variable(value_range = "employees", allows_unassigned = true)]
    pub employee_id: Option<usize>,
}

#[planning_solution(constraints = "crate::constraints::define_constraints")]
pub struct EmployeeSchedule {
    #[problem_fact_collection]
    pub employees: Vec<Employee>,

    #[planning_entity_collection]
    pub shifts: Vec<Shift>,

    #[planning_score]
    pub score: Option<HardSoftScore>,
}
```

What matters here:

- `#[problem_fact]` marks immutable input data
- `#[planning_entity]` marks structs whose variables the solver will change
- `#[planning_variable(value_range = "employees")]` tells SolverForge to use the
  `employees` field on the planning solution as the value source
- `allows_unassigned = true` means a shift can temporarily remain unassigned
- `#[planning_solution(constraints = "...")]` wires the domain to your
  constraint function and generates `Solvable`, `Analyzable`, and typed stream
  accessors such as `factory.shifts()`

## Writing Constraints

The current stream surface uses generated accessors and unified `.join(...)`.
There is no `#[basic_variable_config]`, and self-joins no longer go through
`for_each_unique_pair`.

```rust
use solverforge::prelude::*;
use solverforge::stream::{joiner::*, ConstraintFactory};

pub fn define_constraints() -> impl ConstraintSet<EmployeeSchedule, HardSoftScore> {
    use EmployeeScheduleConstraintStreams;
    use ShiftUnassignedFilter;

    let factory = ConstraintFactory::<EmployeeSchedule, HardSoftScore>::new();

    let unassigned = factory
        .clone()
        .shifts()
        .unassigned()
        .penalize_hard()
        .named("Unassigned shift");

    let missing_skill = factory
        .clone()
        .shifts()
        .filter(|shift: &Shift| shift.employee_id.is_some())
        .join((
            |s: &EmployeeSchedule| &s.employees,
            equal_bi(
                |shift: &Shift| shift.employee_id,
                |employee: &Employee| Some(employee.id),
            ),
        ))
        .filter(|shift: &Shift, employee: &Employee| {
            !employee.skills.contains(&shift.required_skill)
        })
        .penalize_hard()
        .named("Missing skill");

    let no_overlap = factory
        .shifts()
        .join(equal(|shift: &Shift| shift.employee_id))
        .filter(|a: &Shift, b: &Shift| {
            a.id < b.id
                && a.employee_id.is_some()
                && a.start < b.end
                && b.start < a.end
        })
        .penalize_hard()
        .named("No overlap");

    (unassigned, missing_skill, no_overlap)
}
```

Read this as:

- `factory.shifts()` starts from the `shifts` collection generated by
  `EmployeeScheduleConstraintStreams`
- `.unassigned()` is generated automatically because `Shift` has one
  `Option<_>` planning variable
- Cross-collection joins use `equal_bi(...)` inside `.join((extractor, joiner))`
- Self-joins use `.join(equal(...))`
- To keep self-joins unique, filter with a stable ordering such as `a.id < b.id`
- `.named("...")` is the finalizer for a constraint chain

### Why `for_each_unique_pair` Is Gone

Older examples used `for_each_unique_pair(...)` for overlap constraints. In the
current API, you express the same idea as a self-join:

```rust
factory
    .shifts()
    .join(equal(|shift: &Shift| shift.employee_id))
    .filter(|a: &Shift, b: &Shift| a.id < b.id && a.start < b.end && b.start < a.end)
```

That keeps the surface consistent: all pair formation goes through `join(...)`.

## Running the Solver

The stock runtime loads `solver.toml` automatically when solving. A minimal
starting configuration looks like this:

```toml
[termination]
seconds_spent_limit = 30
unimproved_seconds_spent_limit = 5

[[phases]]
type = "construction_heuristic"
construction_heuristic_type = "first_fit"

[[phases]]
type = "local_search"

[phases.acceptor]
type = "late_acceptance"
late_acceptance_size = 400
```

Then solve with a static `SolverManager` and consume `SolverEvent` values:

```rust
use solverforge::{SolverEvent, SolverManager};

static MANAGER: SolverManager<EmployeeSchedule> = SolverManager::new();

fn solve(problem: EmployeeSchedule) {
    let (job_id, mut rx) = MANAGER.solve(problem).expect("solver job should start");

    while let Some(event) = rx.blocking_recv() {
        match event {
            SolverEvent::Progress { metadata } => {
                println!(
                    "current: {:?}, best: {:?}",
                    metadata.current_score,
                    metadata.best_score
                );
            }
            SolverEvent::BestSolution { metadata, .. } => {
                println!("new best at snapshot {:?}", metadata.snapshot_revision);
            }
            SolverEvent::Completed { metadata, .. } => {
                println!("finished with reason {:?}", metadata.terminal_reason);
                break;
            }
            SolverEvent::Cancelled { .. } => break,
            SolverEvent::Failed { error, .. } => panic!("solve failed: {error}"),
            SolverEvent::PauseRequested { .. }
            | SolverEvent::Paused { .. }
            | SolverEvent::Resumed { .. } => {}
        }
    }

    MANAGER.delete(job_id).expect("delete retained job");
}
```

Important details:

- `solve()` returns immediately and runs the search on Rayon
- The channel item type is `SolverEvent<EmployeeSchedule>`, not `(solution, score)`
- `Completed` carries the final owned best solution; `Cancelled` and `Failed` are
  terminal too
- `PauseRequested`, `Paused`, and `Resumed` appear when you drive lifecycle
  controls with `pause()` and `resume()`
- Delete the retained terminal job with `delete(job_id)` when you are done with
  its snapshots and analysis

## Tuning Search

`solver.toml` is where you change runtime behavior without touching Rust code.
Typical knobs:

- `termination.seconds_spent_limit`
- `termination.unimproved_step_count_limit`
- `random_seed`
- `move_thread_count`
- `phases[*].acceptor`
- `phases[*].move_selector`

For example, a local-search phase with an explicit selector:

```toml
[[phases]]
type = "local_search"

[phases.acceptor]
type = "tabu_search"
entity_tabu_size = 7

[phases.move_selector]
type = "change_move_selector"
variable_name = "employee_id"
```

For list-heavy models, nearby search is expressed by picking a nearby selector
variant such as `nearby_list_change_move_selector` or
`nearby_list_swap_move_selector`.

When runtime policy needs per-problem tweaks, use
`#[planning_solution(config = "...")]` to decorate the loaded `solver.toml`
config instead of replacing it outright.

## Useful Generated Surface

The macros generate several helpers that are worth leaning on:

- `{Solution}ConstraintStreams` for typed `ConstraintFactory` accessors such as
  `.employees()` and `.shifts()`
- `{Entity}UnassignedFilter` for `.unassigned()` on matching streams
- `Solvable` when the planning solution declares `constraints = "..."`
- `Analyzable` for score breakdowns through `analyze()`

That generated surface is the main reason the current docs prefer
`factory.shifts()` over repeating `for_each(|s| &s.shifts)` everywhere.

## Quick Reference

- Use `#[planning_variable(value_range = "employees", allows_unassigned = true)]`
  on standard variables
- Use `#[planning_list_variable(element_collection = "visits")]` plus `Vec<usize>`
  for stock list variables
- Add `config = "..."` on `#[planning_solution]` when a solve should layer
  per-solution runtime policy on top of `solver.toml`
- Use `.join(equal(...))` for self-joins and `.join((extractor, equal_bi(...)))`
  for cross-joins
- Use `.named("...")`, not `as_constraint`
- Use `SolverEvent::{Progress, BestSolution, Completed}` as the main happy-path
  events, and handle `Cancelled` / `Failed` for terminal cleanup
- Tune search in `solver.toml`

## Next Steps

- [Configuration](/docs/solverforge/solver/configuration/) for the full config surface
- [Constraint Streams](/docs/solverforge/constraints/constraint-streams/) for stream operators
- [Planning Entities](/docs/solverforge/modeling/planning-entities/) for field-level attributes
- [SolverManager](/docs/solverforge/solver/solver-manager/) for lifecycle management
